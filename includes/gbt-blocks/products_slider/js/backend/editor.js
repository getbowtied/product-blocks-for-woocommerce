(function( $ ) {

    'use strict';

  //   $( document ).ready( function( $ ) {

  //       $(document).on('click', '.editor-block-list__block[data-type="getbowtied/products-slider"] .wp-block-gbt-products-slider .search-result', function(e){
  //       	var selected = $(this).attr('data-id');
  //       	var target   = $('#product-ids-select');
		// 	console.log(selected);
		// 	console.log(target.val());

		// 	var event = new Event('change');
		// 	var element = document.getElementById('product-ids-select');
		// 	element.dispatchEvent(event);
		// });
  //   }); 

} )( jQuery );